#1	Crear una lista con las notas de 10 estudiantes. • Mostrar la lista completa. 
#• Calcular y mostrar el promedio. • Indicar la nota más alta y la más baja.
# Crear la lista 
notas = [7, 8, 6, 9, 5, 8, 7, 6, 10, 7]

# Mostrar la lista completa
for nota in notas:
    print(nota)

# Calcular y mostrar el promedio
promedio = sum(notas) / len(notas)
print("Promedio:", promedio)

# Mostrar la nota más alta y la más baja
print("Nota más alta:", max(notas))
print("Nota más baja:", min(notas))

#2. 	Pedir al usuario que cargue 5 productos en una lista. • Mostrar la lista ordenada 
#alfabéticamente. Investigue el uso del método sorted(). • Preguntar al usuario qué producto desea eliminar y actualizar la lista.
# Crear lista vacía
productos = []

# Pedir al usuario que cargue 5 productos
for i in range(5):
    nombre = input("Ingresá un producto: ")
    productos.append(nombre)

# Mostrar la lista ordenada alfabéticamente
productos_ordenados = sorted(productos)
print("Lista ordenada:")
for producto in productos_ordenados:
    print(producto)

# Preguntar qué producto eliminar
eliminar = input("¿Qué producto querés eliminar?: ")

# Eliminar y mostrar lista actualizada
if eliminar in productos:
    productos.remove(eliminar)
    print("Lista actualizada:")
    for producto in productos:
        print(producto)
else:
    print("Ese producto no está en la lista.")

#3. 	Generar una lista con 15 números enteros al azar entre 1 y 100. • Crear una lista con los pares
# y otra con los impares. • Mostrar cuántos números tiene cada lista.
import random

# Generar lista 
numeros = []
for i in range(15):
    numeros.append(random.randint(1, 100))

# Crear listas de pares e impares
pares = []
impares = []

for numero in numeros:
    if numero % 2 == 0:
        pares.append(numero)
    else:
        impares.append(numero)

# Mostrar cuántos números tiene cada lista
print("Lista completa:", numeros)
print("Cantidad de pares:", len(pares))
print("Cantidad de impares:", len(impares))


#4. 	Dada una lista con valores repetidos: • Crear una nueva lista sin elementos repetidos. 
#• Mostrar el resultado.
valores = [1, 3, 5, 3, 7, 1, 9, 5, 3]

# Crear lista sin repetidos
sin_repetidos = []
for valor in valores:
    if valor not in sin_repetidos:
        sin_repetidos.append(valor)

print("Lista sin repetidos:", sin_repetidos)

#5. 	Crear una lista con los nombres de 8 estudiantes presentes en clase. 
#• Preguntar al usuario si quiere agregar un nuevo estudiante o eliminar uno existente. • Mostrar la lista final actualizada

# Lista con 8 estudiantes
estudiantes = ["Ana", "Luis", "María", "Pedro", "Sofía", "Juan", "Lucía", "Carlos"]

#  agregar o eliminar estudiante
accion = input("¿Querés agregar o eliminar un estudiante? (agregar/eliminar): ")

if accion == "agregar":
    nuevo = input("Nombre del nuevo estudiante: ")
    estudiantes.append(nuevo)
elif accion == "eliminar":
    borrar = input("Nombre del estudiante a eliminar: ")
    if borrar in estudiantes:
        estudiantes.remove(borrar)
    else:
        print("Ese estudiante no está en la lista.")

# Mostrar lista final
print("Lista actualizada:")
for estudiante in estudiantes:
    print(estudiante)

#6. 	Dada una lista con 7 números, rotar todos los elementos una posición hacia la derecha 
#(el último pasa a ser el primero).

numeros = [10, 20, 30, 40, 50, 60, 70]

# Rotar una posición a la derecha
ultimo = numeros[-1]
numeros = [ultimo] + numeros[:-1]

# Mostrar la lista rotada
print("Lista rotada:", numeros)

#7. 	Crear una matriz (lista anidada) de 7x2 con las temperaturas mínimas y máximas de una semana.
#• Calcular el promedio de las mínimas y el de las máximas. • Mostrar en qué día se registró la mayor amplitud térmica.

# Crear matriz 
temperaturas = [
    [10, 22],
    [12, 25],
    [9, 20],
    [11, 24],
    [13, 27],
    [8, 19],
    [10, 23]
]

# Calcular promedio 
suma_min = 0
suma_max = 0

for dia in temperaturas:
    suma_min += dia[0]
    suma_max += dia[1]

promedio_min = suma_min / 7
promedio_max = suma_max / 7

print("Promedio mínimas:", promedio_min)
print("Promedio máximas:", promedio_max)

# Buscar día con mayor amplitud térmica
mayor_amplitud = 0
dia_mayor = 0

for i in range(7):
    amplitud = temperaturas[i][1] - temperaturas[i][0]
    if amplitud > mayor_amplitud:
        mayor_amplitud = amplitud
        dia_mayor = i + 1  # Día 1 a 7

print("Mayor amplitud térmica en el día:", dia_mayor)

#8. 	Crear una matriz con las notas de 5 estudiantes en 3 materias. • 
#Mostrar el promedio de cada estudiante. • Mostrar el promedio de cada materia.
# Matriz con notas de 5 estudiantes en 3 materias

notas = [
    [7, 8, 6],   # Estudiante 1
    [5, 6, 7],   # Estudiante 2
    [9, 8, 10],  # Estudiante 3
    [6, 7, 5],   # Estudiante 4
    [8, 9, 7]    # Estudiante 5
]

# Promedio de cada estudiante
print("Promedios por estudiante:")
for fila in notas:
    promedio = sum(fila) / len(fila)
    print(promedio)

# Promedio de cada materia
print("Promedios por materia:")
for i in range(3):  # 3 materias
    suma = 0
    for j in range(5):  # 5 estudiantes
        suma += notas[j][i]
    promedio = suma / 5
    print(promedio)

#9. 	Representar un tablero de Ta-Te-Ti como una lista de listas (3x3). 
#• Inicializarlo con guiones "-" representando casillas vacías.
 #• Permitir que dos jugadores ingresen posiciones (fila, columna) para colocar "X" o "O". • Mostrar el tablero después de cada jugada.

# tablero vacío 
tablero = [
    ["-", "-", "-"],
    ["-", "-", "-"],
    ["-", "-", "-"]
]

# Mostrar  tablero
def mostrar_tablero():
    for fila in tablero:
        print(" ".join(fila))

# Jugadas de dos jugadores
for turno in range(2):
    jugador = "X" if turno == 0 else "O"
    print("Turno del jugador", jugador)
    fila = int(input("Ingresá la fila (0 a 2): "))
    columna = int(input("Ingresá la columna (0 a 2): "))
    tablero[fila][columna] = jugador
    mostrar_tablero()


#10. 	Una tienda registra las ventas de 4 productos durante 7 días, en una matriz de 4x7. 
#• Mostrar el total vendido por cada producto. • Mostrar el día con mayores ventas totales. 
# Indicar cuál fue el producto más vendido en la semana. vamos 1 por una explicándome que vas haciando.
# voy hacerlo en visualcode para después subirlo a github

# Matriz 4x7 con ventas (4 productos, 7 días)
ventas = [
    [10, 12, 8, 15, 9, 11, 13],   # Producto 1
    [7, 9, 6, 10, 8, 7, 9],       # Producto 2
    [14, 13, 15, 12, 16, 14, 15], # Producto 3
    [5, 6, 4, 7, 5, 6, 5]         # Producto 4
]

# Total vendido por cada producto
print("Total por producto:")
for fila in ventas:
    total = sum(fila)
    print(total)

# Día con mayores ventas totales
mayor_total = 0
dia_mayor = 0

for dia in range(7):
    suma_dia = 0
    for producto in range(4):
        suma_dia += ventas[producto][dia]
    if suma_dia > mayor_total:
        mayor_total = suma_dia
        dia_mayor = dia + 1  # Día 1 a 7

print("Día con mayores ventas:", dia_mayor)

# Producto más vendido en la semana
mayor_producto = 0
indice_producto = 0

for i in range(4):
    total = sum(ventas[i])
    if total > mayor_producto:
        mayor_producto = total
        indice_producto = i + 1  # Producto 1 a 4

print("Producto más vendido:", indice_producto)
